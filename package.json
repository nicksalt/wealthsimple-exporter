{
  "name": "wealthsimple-exporter",
  "version": "0.1.0",
  "private": true,
  "description": "Chrome extension to export Wealthsimple transactions to CSV/OFX/QFX",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "test": "vitest",
    "zip": "zip -r wealthsimple-exporter.zip dist"
  },
  "devDependencies": {
    "@crxjs/vite-plugin": "^2.0.0-beta.23",
    "@types/chrome": "^0.0.268",
    "jsdom": "^28.0.0",
    "typescript": "^5.0.0",
    "vite": "^5.0.0",
    "vitest": "^1.0.0"
  }
}
